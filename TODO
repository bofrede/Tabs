Add AutoFocus, to let a tab take focus, if an input field receives focus.
Used when form validation sets focus to a field that wasn't properly filled in.

The anonymous function declared in line 100 is declared in a loop.
Name the function or put the function in a closure.

setTabFromAnchor can set a tab active on load, if the url ends with a hash.
